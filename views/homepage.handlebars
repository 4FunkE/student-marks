<!-- Loop over each object in the projects array -->
{{#each ratings as |rating| }}
<div class="row mb-4 rating">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/rating/{{rating.id}}">{{rating.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="apple">🍎</span>
      <span class="teacher-rating">{{rating.teacher_rating}}</span>
      out of 5
    </p>
    <p>Created by {{rating.user.name}} on {{format_date rating.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{rating.description}}
    </p>
  </div>
</div>
{{/each}}
